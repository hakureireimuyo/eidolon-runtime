# 虚拟环境（本运行时独立持有 venv，见 docs/environment-isolation.md）
.venv/

# 用户数据（角色包、上传、对话产物）——与源码严格分离
workspace/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage

# 本地密钥
.env
config.toml

# 本机双击启动器（PowerShell 下 bash 会被解析为 WSL bash，仅本机环境需要）
start-runtime.bat
